/* 
1) Build a simple loop, that print numbers to the console. from 0 to the lenght of an array.

*/

const numArr = ["red", "blue", "yellow", "green", "white", "purple", "black"];

for (let inc = 0; inc < numArr.length; inc++) {
  console.log(inc);
}
